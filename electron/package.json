{
  "name": "euphonia-electron",
  "private": true,
  "version": "0.2.0",
  "description": "Euphonia desktop app — voice-training recorder and analyzer",
  "author": "Euphonia contributors",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -b",
    "start": "npm run build && electron .",
    "dist": "npm run build && electron-builder"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  }
}
