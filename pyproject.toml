[project]
name = "voice-training"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.0",
    "praat-parselmouth>=0.4.7",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.21.0",
]
